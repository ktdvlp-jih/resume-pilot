# AI 오케스트레이션 순서도 (2026-07-09)

## 1) 요청하신 형태 (양갈래 ASCII)

```text
                 React (resume-web / resume-admin)
                           |
                           |
                    Spring Boot (resume-api)
                           |
                ----------------------
                |                    |
         PostgreSQL 17         FastAPI (resume-ai)
          + pgvector                |
                |              RAG Engine
                |             (rag-service)
                |                  |
                |            Prompt Engine
                |         (prompt-service)
                |                  |
                ----------------------
                           |
                    LLM (OpenAI)
```

---

## 2) 프로세스 기준 순서도 (실행 순서)

```text
React (resume-web / resume-admin)
            |
            v
Spring Boot (resume-api)
            |
            v
AI Gateway Orchestrator (resume-ai)
            |
            v
RAG Engine (rag-service)
  - vector search
  - context build
            |
            v
Prompt Engine (prompt-service)
  - prompt version load
  - system/user prompt render
            |
            v
LLM Router (OpenAI / model selection)
            |
            v
Postprocess + Validation
            |
            v
ai_usage_logs write
            |
            v
API response to client
```

---

## 3) 업무별 실행 차이

```text
[A] 생성
Gateway -> RAG -> Prompt -> LLM -> 후처리

[B] 첨삭/탐지
Gateway -> Prompt -> LLM -> 후처리
(* RAG 생략 가능)

[C] 공고분석
Gateway -> Prompt(JOB_ANALYSIS v8) -> LLM
        -> 실패 시 rule fallback -> 후처리
(* RAG 일반적으로 미사용)
```

