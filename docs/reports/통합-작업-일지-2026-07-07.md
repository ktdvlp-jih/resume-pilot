# 통합 작업 일지 (2026-07-07)

> 기준: 2026-07-07 당일 대화/운영 서버 작업/깃 커밋 내역 통합

## 운영자용 5줄 요약

1. 오늘 배포 라인은 최종적으로 `TC-AI-05` 포함 전부 PASS 상태로 정리됨.
2. 운영 서버에서 Gemini 임베딩 설정 변경 후 발생한 추천 0건 이슈를 코드 핫픽스로 해결함.
3. `rag-service` 임베딩 차원 강제/보정 로직 반영 후 AI E2E 재검증 PASS 확인함.
4. R8(사용자/관리자 가이드)은 완료, R9(보안·부하)는 1차 점검 완료 상태로 갱신함.
5. Sentry/Named Tunnel은 현재 정책상 보류·패스로 유지하고, 남은 건 R9 심화 점검임.

## 1. 오늘의 핵심 결과

- 배포 파이프라인 안정화 완료 (HTTP/API/E2E/AI E2E)
- 이미지 공고 분석(OCR+Vision/LLM 보강) 반영
- `TC-AI-05` AI E2E를 CI에 편입하고 운영 재검증 완료
- 사용자/관리자 가이드 및 보안·부하 1차 점검 문서화 완료
- Gemini 임베딩 차원 이슈(추천 0건) 핫픽스 완료

## 2. 대화 기반 의사결정

- Sentry는 당분간 보류 (`R5` 보류)
- Named Tunnel은 우선 패스 유지
- Gemini를 계속 사용하되 임베딩 안정성 확보 방향으로 진행
- 배포 후 AI E2E 실패 시 원인 파악 → 즉시 코드 수정/재배포 원칙 합의

## 3. 운영 서버 직접 작업(사용자 수행 포함)

다음 작업이 운영 환경에서 실제 수행됨:

1. 서버 `.env` 수정
   - `EMBEDDING_MODEL=gemini-embedding-001`
   - `EMBEDDING_DIMENSION=1536`
2. 재배포 실행
3. `deploy-smoke-e2e-ai.sh` 실행 중 실패 확인
   - 오류: `RAG recommend returned no experiences`
4. 수정 배포 후 재실행
   - `tests/ai-flow.spec.ts` PASS 확인

## 4. 기술 이슈 및 해결

### 이슈 A: AI E2E 실패 (`RAG recommend returned no experiences`)

- 원인: Gemini 임베딩 응답 차원과 DB `vector(1536)` 저장 불일치 가능
- 영향: 임베딩 저장 실패 → 추천 0건 → `TC-AI-05` 실패
- 조치:
  - 임베딩 호출에 `dimensions` 전달 시도
  - 실패 시 raw 호출 재시도
  - 반환 벡터 길이를 `EMBEDDING_DIMENSION`에 맞게 강제 보정(자르기/패딩)
  - 최종 실패 시 hash fallback 유지
- 결과: 운영 재배포 후 AI E2E PASS

### 이슈 B: user journey E2E strict mode 실패

- 원인: 모바일/데스크톱 레이아웃 동시 마운트로 동일 `testid` 중복
- 조치: 뷰포트별 단일 렌더링 구조로 수정
- 결과: user journey PASS

## 5. 당일 주요 커밋(요약)

| SHA | 유형 | 요약 |
|---|---|---|
| `febd47a` | feat | 이미지 공고 OCR/vision + UX + 문서 동기화 |
| `528904a` | fix | user-journey CI, AuthIntegrationTest, 레이아웃 중복 수정 |
| `ca9ad7a` | feat | `TC-AI-05` Playwright AI E2E + Gemini fallback |
| `4ca9af5` | docs | 사용자 가이드 + 보안/부하 점검 리포트 |
| `c9d44d9` | docs | R8 완료, R9 1차 상태 반영 |
| `ad6d3ab` | fix | Gemini 임베딩 차원 강제/정규화 핫픽스 |
| `7c0423d` | docs | docs 파일명 한글화 + 링크 전면 갱신 |
| `94e1704` | docs | AI E2E 기준/일일 보고 추가 및 문서 최신화 |

## 6. 검증 결과

- `deploy-smoke.sh` PASS (HTTP/API)
- `deploy-smoke-e2e.sh` PASS (smoke + user journey)
- `deploy-smoke-e2e-ai.sh` 최종 PASS (`TC-AI-05`)
- 보안·부하 1차:
  - 무토큰 인증 401 정상
  - CORS preflight 정상
  - `/actuator/health` 60회: 성공 60/60, avg 60.9ms, p95 62ms

## 7. 남은 과제(실질)

1. `R9` 잔여: 취약점 스캔, k6 부하, rate limit 정책화
2. `배포-테스트-케이스` 결과표 전체 주기 업데이트
3. Sentry 보류 상태 유지(필요 시 재개)

---

## 8. 2026-07-08 후속 반영

- LLM Provider 통합 운영:
  - `llm_providers`, `llm_model_routes` 적용
  - Admin `/admin/llm-settings`에서 Provider 키/라우트 관리
  - GitHub Models provider 추가
- failover 정책 보강:
  - 401/403/429/5xx 모두 다음 Provider로 전환
- rag-service 통합:
  - `.env` 의존 축소, 내부 runtime-config 기반 EMBEDDING 라우트 사용
- AI 로그 개선:
  - 공고분석(`job_analysis`) 로그 누락 보완
  - AI 로그 조회 경량화(최근 30건 projection)
  - 모델명 컬럼 추가(친화명 + 모델 ID 병기)

